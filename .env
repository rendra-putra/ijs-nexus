# VITE_API_BASE_URL=https://ipas-gateway-dev.bps.go.id
# VITE_API_ENGINE_URL=https://ipas-gateway-dev.bps.go.id

# We commented out the direct URLs because for local development, 
# it's better to use Vite's proxy (configured in vite.config.js) 
# to avoid CORS (net::ERR_FAILED) issues.
